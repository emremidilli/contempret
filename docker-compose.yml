version: "3.9"

services:
  app_contempret:
    image: app_contempret:latest
    build:
      context: .
    volumes:
      - ./bin:/app/bin
    command: sleep infinity
    deploy:
      resources:
        reservations:
          devices:
          - driver: nvidia
            count: -1
            capabilities: [gpu]
